# HTML-CSS-ISMAEL-FIGUEROA
Fines académicos
Test

# PASO 0 
1.- Descargar e instalar Git
2.- VSC
3.- Crear Directorioy abrir con VSC
4.- Abrir terminal y probar los comandos:
    git --v (para version d git)
    git config --global user.name 'nombre' para agregar username
    got config --global user.email 'correo' self-explain lol
5.- Clonar el repositorio de GitHub: 
    git clone 'url'

# PASO 1
Cada cambio que hacemos dentro del proyecto local cuando queremos guardar debemos hacer lo sgte:
    git add . (guardar todo)
    git commit -m 'comentario' (crea punto d guardado)
    git push origin main (sube los cambios a la nube)